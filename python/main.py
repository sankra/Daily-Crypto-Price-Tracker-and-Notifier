import os
import csv
import sys
import requests
import sqlalchemy

for i in range(3):
    print(f"sys.path[{i}]: {sys.path[i]}")
    print(f"sys.modules[{i}]: {sys.modules[i]}")
    print(f"sys.modules[{i}]: {sys.modules[i]}")